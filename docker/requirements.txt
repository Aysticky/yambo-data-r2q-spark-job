"""
Python dependencies for Spark jobs

This file is generated from pyproject.toml for Docker builds.
To update:
  poetry export --without-hashes --format=requirements.txt > docker/requirements.txt

DEPENDENCY NOTES:
- pyspark: Spark framework (match EKS Spark Operator version)
- boto3: AWS SDK (S3, DynamoDB, Secrets Manager)
- requests: HTTP client for API calls
- stripe: Official Stripe SDK (easier than raw REST)
- tenacity: Retry library with exponential backoff
- pyarrow: Arrow/Parquet support for Spark

VERSION PINNING STRATEGY:
- Pin major.minor for stability (3.5.x)
- Allow patch updates (security fixes)
- Test upgrades in dev before prod
"""

pyspark>=3.5.0,<3.6.0
boto3>=1.34.0,<2.0.0
requests>=2.31.0,<3.0.0
stripe>=7.0.0,<8.0.0
tenacity>=8.2.0,<9.0.0
pyarrow>=14.0.0,<15.0.0
python-dateutil>=2.8.0,<3.0.0
